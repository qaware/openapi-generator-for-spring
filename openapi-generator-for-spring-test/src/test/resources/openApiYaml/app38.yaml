openapi: 3.0.1
info:
  title: API for App38
  version: unknown
paths:
  /mapping1:
    get:
      operationId: mapping1
      parameters:
        - name: param1
          in: query
          required: true
          schema:
            type: string
      requestBody:
        content:
          '*/*':
            schema:
              type: string
          text/plain;charset=UTF-8:
            schema:
              type: string
        required: true
      responses:
        "200":
          description: Default response
          content:
            text/plain;charset=UTF-8:
              schema:
                type: string
            text/event-stream:
              schema:
                type: string